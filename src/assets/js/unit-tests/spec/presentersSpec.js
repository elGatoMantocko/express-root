describe('App.Presenters', function() {
  beforeEach(function() {
  });

  // TODO: tests here
  describe('#Home()', function() {
    it('should be a function', function() {
      assert.isFunction(App.Presenters.Home);
    });
  });

  describe('#Input()', function() {
    it('should be a function', function() {
      assert.isFunction(App.Presenters.Input);
    });
  });

  afterEach(function() {
  });
});
